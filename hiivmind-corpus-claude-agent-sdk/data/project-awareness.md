# Claude Agent SDK Documentation Corpus

A curated documentation corpus for the Claude Agent SDK is available. Use the **hiivmind-corpus-claude-agent-sdk-navigate** skill when you need to look up Claude Agent SDK documentation.

## When to Use

- Building AI agents using the Claude Agent SDK
- Working with the query(), tool(), or createSdkMcpServer() functions
- Implementing hooks, subagents, or MCP integration
- Need accurate, up-to-date Claude Agent SDK documentation

## How to Invoke

Say something like:
- "Look up Claude Agent SDK docs for..."
- "Check the Agent SDK corpus for..."
- "What does the Claude Agent SDK documentation say about..."

## Key Topics

- TypeScript and Python SDK references
- Built-in tools (Read, Write, Edit, Bash, Glob, Grep, WebSearch, WebFetch)
- Hooks (PreToolUse, PostToolUse, Stop, etc.)
- Subagents and multi-agent patterns
- MCP server integration
- Permissions and sandboxing
- Sessions and multi-turn conversations

## Example Questions

- "How do I create a custom tool in the Agent SDK?"
- "What hooks are available in the Claude Agent SDK?"
- "How do I use MCP servers with the Agent SDK?"
- "What's the difference between query() and tool()?"
